<h1>ユーザー一覧</h1>

<table class="table">
  <tr>
    <th></th>
    <th>ユーザー名</th>
    <th>フォロー数</th>
    <th>フォロワー数</th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
    <td><%= image_tag user.get_profile_image(50,50) %></td>
    <td><%= user.name %></td>
    <td><%=  %></td>
    <td><%=  %></td>
    <td><%= link_to "詳細", user_path(user) %></td>
  <% end %>
</table>

