<%= form_with url: search_path, method: :get do |f| %>
  <%= f.text_field :word %>
  <% if request.url.include?("user") %>
    <%= f.submit "検索", name: "user_search" %>
  <% else %>
    <%= f.submit "検索", name: "note_search" %>
  <% end %>
<% end %>